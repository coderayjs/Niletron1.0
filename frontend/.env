# Copy to .env.local for local overrides (not committed).
# For production build (Vercel, Netlify, Render static): set VITE_API_URL in the host’s env UI.

# Render backend — no trailing slash, no /api suffix
VITE_API_URL=https://niletron1-0.onrender.com
